!function($) {
    $.jqplot.DivTitleRenderer = function() {}, $.jqplot.DivTitleRenderer.prototype.init = function(options) {
        $.extend(!0, this, options);
    }, $.jqplot.DivTitleRenderer.prototype.draw = function() {
        this._elem && (this._elem.emptyForce(), this._elem = null);
        var elem = (this.renderer, document.createElement("div"));
        if (this._elem = $(elem), this._elem.addClass("jqplot-title"), this.text) {
            if (this.text) {
                var color;
                this.color ? color = this.color : this.textColor && (color = this.textColor);
                var styles = {
                    position: "absolute",
                    top: "0px",
                    left: "0px"
                };
                this._plotWidth && (styles.width = this._plotWidth + "px"), this.fontSize && (styles.fontSize = this.fontSize), 
                "string" == typeof this.textAlign ? styles.textAlign = this.textAlign : styles.textAlign = "center", 
                color && (styles.color = color), this.paddingBottom && (styles.paddingBottom = this.paddingBottom), 
                this.fontFamily && (styles.fontFamily = this.fontFamily), this._elem.css(styles), 
                this.escapeHtml ? this._elem.text(this.text) : this._elem.html(this.text);
            }
        } else this.show = !1, this._elem.height(0), this._elem.width(0);
        return elem = null, this._elem;
    }, $.jqplot.DivTitleRenderer.prototype.pack = function() {};
}(jQuery);
//# sourceMappingURL=jqplot.divTitleRenderer.min.js.map